---
const { trail } = Astro.props; // array of {label, href}
---
<nav class="text-sm text-muted mb-3">
  {trail.map((t,i) => (
    <span>
      <a href={t.href} class="text-accent">{t.label}</a>
      {i < trail.length-1 ? ' → ' : ''}
    </span>
  ))}
</nav>
